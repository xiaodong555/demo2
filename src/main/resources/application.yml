server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springboot_db
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: rootmysql

mybatis:
  type-aliases-package: cn.itcast.domain
  mapper-locations:
    - calsspath: mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level: debug